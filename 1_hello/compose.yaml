services:
        webserver:
                build:
                        context: .
                        dockerfile: Dockerfile
                ports:
                        - "8080:80"
                container_name: static_website
